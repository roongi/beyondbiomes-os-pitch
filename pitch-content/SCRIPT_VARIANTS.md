# Beyond Biomes — Script Variants & Multi-Model Comparison
_For the training app's "listen · skip · decide" selector. Generated by running the pitch through Codex + Gemini CLIs + Claude._

## Status of the 3 model reviews
| Model | Tool | Result |
|---|---|---|
| **Claude** | (this session) | v1 (`PITCH_MASTER_v1.md`) + **v2 blend** (`PITCH_MASTER_v2.md`) |
| **Codex** | `codex exec` (gpt-5-codex, WSL) | ✅ critique + improved script (`codex_improved.md`) |
| **Gemini** | `gemini` CLI (WSL) | ⛔ **API key EXPIRED** — renew `GEMINI_API_KEY` in WSL, then re-run (command below) |

**Re-run Gemini after renewing the key:**
`wsl -d Ubuntu-new -- bash -lc 'cd /mnt/e/WSL/shared/pitches/_pitch_build && GEMINI_CLI_TRUST_WORKSPACE=true gemini < review_prompt.txt > gemini_review.out'`

## The variants to load into the trainer (listen · skip · pick per section)
| id | Source | Character | Best for |
|---|---|---|---|
| `claude_v1` | PITCH_MASTER_v1 | Warm narrative, "what a time to be alive" open | Emotional/ecosystem jurors |
| `claude_v2` ⭐ | PITCH_MASTER_v2 | Blend: "what if" open, tightest + most honest | **Recommended default** |
| `codex` | codex_improved.md | Crisp, metaphor-forward, remapped slides | VC-heavy room |
| `winner_soft` | BeyondBiomes_Pitch_FINAL_draft ("Eyes before muscle") | Soft, no hard numbers | When you want pure story |
| `deck_180` | lastpitchsession 180s | Fully-numbered, deck-locked | Number-heavy defense |

## What Codex changed (all folded into claude_v2)
1. **"What if" bookend** beats "what a time to be alive" for memorability.
2. **Slide 4 honesty fix:** don't say you "treat" — *diagnose & prescribe; the producer delivers the cure.* (Protects "we sell the eyes; they sell the muscle.")
3. "fifteen thousand" → **"over fifteen thousand."**
4. **Add the OpenAPI line** (~890 endpoints today + contract next).
5. **600 → 625** metagenomes (consistency with the funded trial).
6. **Add a compact market line** (€12.5B / €3.5B) — one line, no spreadsheet theatre.

## Recommendation
Default to **`claude_v2`** for the live pitch; keep **`codex`** loaded as the VC-room alternative and **`winner_soft`** for warmth. In the trainer, play all three per section, mark ♥ on the line you like best, and export your personal blend. Add **`gemini`** as a 4th once the key is renewed.

---
## CODEX IMPROVED SCRIPT (verbatim, for reference)
(See `codex_improved.md` for the clean copy; critique is in §"What Codex changed".)
